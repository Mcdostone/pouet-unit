# Agent.md

## Dev environment tips
- Use `cargo clippy --fix --allow-dirty --all-targets --all-features` to automatically fix linting issues in Rust code.
- Use `cargo fmt --all` to format Rust code according to standard style guidelines.
